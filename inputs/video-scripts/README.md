# Video Script Library

Store each video idea as a Markdown script in the matching folder:

- `short-format/` for production-ready short-form scripts.
- `long-format/` for production-ready long-form scripts.
- `testing/` for scripts used while testing the production pipeline.

The simple short-project launcher uses `testing/new-video-script.md` when no script path is entered.
